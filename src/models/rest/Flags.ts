/* istanbul ignore file */
/* tslint:disable */
/* eslint-disable */

/**
 * Флаги, которые необходимо обновить
 */
export type Flags = {
    categories?: Array<number>;
    canDownload?: boolean;
    imagesAvailable?: boolean;
    mapLocked?: boolean;
    shortTag?: string;
    mapVerified?: boolean;
};
